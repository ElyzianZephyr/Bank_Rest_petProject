# Bank Card Management System (REST API)

Система управления банковскими картами.
REST API приложение на Java (Spring Boot) для эмуляции работы банковского процессинга. Проект реализует безопасное управление картами, счетами и транзакциями.

## 🚀 Функциональность

* **Аутентификация:** Регистрация и безопасный вход (JWT Access Token).
* **Клиенты:**
    * Просмотр своих карт с маскированием номера (PAN).
    * Переводы между своими счетами.
    * Блокировка карт.
* **Администраторы:**
    * Полный жизненный цикл карт (создание, активация, блокировка, удаление).
    * Управление пользователями (бан/разбан).
* **Безопасность:**
    * Шифрование номеров карт в базе данных (AES-256).
    * Role-Based Access Control (RBAC).

## 🛠 Технологический стек

* **Language:** Java 21
* **Framework:** Spring Boot 3.5.9
* **Security:** Spring Security 6, JWT (JJWT)
* **Database:** PostgreSQL
* **Migration:** Liquibase
* **API Documentation:** SpringDoc OpenAPI (Swagger)
* **Containerization:** Docker & Docker Compose

## ⚙️ Настройка окружения

Для запуска приложения необходимо задать переменные окружения (либо в системе, либо в IDE). Эти переменные используются в `application.yml`.

| Переменная | Описание | Пример генерации (Terminal) |
| :--- | :--- | :--- |
| `APP_ENCRYPTION_KEY` | 32-байтный ключ (Base64) для шифрования карт в БД. | `echo -n "12345678901234567890123456789012" | base64` |
| `JWT_SECRET` | Секретный ключ для подписи JWT токенов. | Любая сложная строка (мин. 32 символа) |

## 🏃 Инструкция по запуску

### 1. Запуск базы данных (Docker Compose)
Для работы приложения требуется PostgreSQL. Поднимите контейнер с базой:

```bash

docker-compose up -d
```
Убедитесь, что порт `5432` свободен.

### 2. Запуск приложения (Локально через Maven)

Установите переменные окружения и запустите приложение:

**Linux / macOS:**

```bash

export APP_ENCRYPTION_KEY="MTIzNDU2Nzg5MDEyMzQ1Njc4OTAxMjM0NTY3ODkwMTI="
export JWT_SECRET="mySuperSecretKeyForJwtSigning12345"

mvn spring-boot:run
```

**Windows (PowerShell):**

```powershell
$env:APP_ENCRYPTION_KEY="MTIzNDU2Nzg5MDEyMzQ1Njc4OTAxMjM0NTY3ODkwMTI="
$env:JWT_SECRET="mySuperSecretKeyForJwtSigning12345"

mvn spring-boot:run
```




## 📚 Документация API

[Описание задачи](README_Task.md)

После успешного запуска документация доступна по адресу:
## Документация API

* [Спецификация OpenAPI (Swagger)](docs/openapi.yaml) — файл для импорта в Postman или Swagger UI.

* **Swagger UI:** [http://localhost:8080/swagger-ui.html](http://localhost:8080/swagger-ui.html)
* **OpenAPI JSON:** [http://localhost:8080/v3/api-docs](http://localhost:8080/v3/api-docs)

Статическая версия спецификации также доступна в файле `docs/openapi.yaml`.

## 🧪 Тестирование

Запуск модульных и интеграционных тестов:

```bash

mvn test
```